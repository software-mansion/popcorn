name: Popcorn JS e2e tests

on:
  push:
    branches: ["**"]
    paths:
      - "js/popcorn/**"
      - "lib/**/wasm.ex"
      - ".github/workflows/popcorn_js_e2e_test.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CI: true

jobs:
  e2e:
    name: JS E2E tests
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-toolchain

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "pnpm"
          cache-dependency-path: js/pnpm-lock.yaml

      - name: Install JS dependencies
        run: pnpm install
        working-directory: js

      - name: Build popcorn JS package
        run: pnpm build:prod
        working-directory: js/popcorn

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-js-popcorn-${{ hashFiles('js/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-playwright-js-popcorn-
            ${{ runner.os }}-playwright-

      - name: Install Playwright browsers
        run: pnpm exec playwright install chromium --with-deps
        working-directory: js/popcorn

      - name: Run e2e tests
        run: pnpm test:e2e
        working-directory: js/popcorn
