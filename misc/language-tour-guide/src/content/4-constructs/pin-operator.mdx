export const frontmatter = { order: 2 };

# Pin operator

Like in all patterns, we can use the pin `^` operator in `case`.

<EditorCode>
```elixir
x = 1

# Try changing 10 to 1
case 10 do
  ^x -> IO.puts "Matched the value of x"
  _ -> IO.puts "Not matched the value of x"
end
```
</EditorCode>
