export const frontmatter = { order: 6 };

# Exercise: Modules and Functions

Time to practice what you've learned about modules, functions, and pattern matching!

## Your Task

Create a `Calculator` module that:

1. Has a function `calculate/3` that takes an operation atom (`:add`, `:subtract`, `:multiply`, `:divide`) and two numbers
2. Uses pattern matching to handle different operations
3. Returns `{:ok, result}` for successful operations
4. Returns `{:error, "Division by zero"}` when dividing by zero
5. Returns `{:error, "Unknown operation"}` for unsupported operations

## Hints

- Use multiple function clauses with pattern matching
- Remember to handle the division by zero case with a guard
- Use tagged tuples for return values

export const defaultCode = `defmodule Calculator do
  # TODO: Implement calculate/3 with pattern matching for :add
  def calculate(:add, a, b) do
    # Your code here
  end
  
  # TODO: Implement pattern matching for :subtract
  
  # TODO: Implement pattern matching for :multiply
  
  # TODO: Implement pattern matching for :divide
  # Hint: Use a guard to check for division by zero
  
  # TODO: Implement a catch-all clause for unknown operations
end

# Test your implementation

IO.inspect(Calculator.calculate(:add, 10, 5)) # Should return {:ok, 15}
IO.inspect(Calculator.calculate(:subtract, 10, 3)) # Should return {:ok, 7}
IO.inspect(Calculator.calculate(:multiply, 4, 7)) # Should return {:ok, 28}
IO.inspect(Calculator.calculate(:divide, 20, 4)) # Should return {:ok, 5.0}
IO.inspect(Calculator.calculate(:divide, 10, 0)) # Should return {:error, "Division by zero"}
IO.inspect(Calculator.calculate(:power, 2, 3)) # Should return {:error, "Unknown operation"}`
